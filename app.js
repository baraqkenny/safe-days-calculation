// function validateSelect() {
//   const selectedInput = document.getElementById("selected-input");
//   const selectError = document.getElementById("select-error");

//   // Check if the selected option's value is empty (default)
//   selectedInput.addEventListener('click', ()=> {
//     if (selectedInput.value === "") {
//     selectError.textContent = "Please select an option.";
//   } else {
//     selectError.textContent = "";
//   }
//   })
  
// }

// validateSelect()

// Submit Button Validation
const submitBtn = document.getElementById('submit-btn');
submitBtn.addEventListener('click', (e)=> {
    e.preventDefault();
    
    // Date Validation Field
    const dateInput = document.querySelector('.date-input');
    dateInput.addEventListener('input', ()=> {

    })
    
    
})